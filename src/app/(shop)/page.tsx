import { titleFont } from "@/config/fonts";

export default function ShopPage() {
  return (
    <main className="">
      <h1 className={`${titleFont.className} text-4xl font-bold`}>Shop Page</h1>
    </main>
  );
}